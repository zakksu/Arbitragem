// Arbitragem Core17 — petr4_cash_scalp
// Structure: scalp · VWAP reclaim scalp on underlying
// Underlying: PETR4
input
  StopTicks(7);
  TargetTicks(9);
var
  EntryPrice : Float;
begin
  EntryPrice := Close;
// scalp PETR4 — VWAP reclaim scalp on underlying
// BuyAtMarket(100, "PETR4");
  SetStopLoss(StopTicks * MinPriceIncrement);
  SetProfitTarget(TargetTicks * MinPriceIncrement);
end;
