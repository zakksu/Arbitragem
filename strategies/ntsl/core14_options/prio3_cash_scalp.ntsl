// Arbitragem Core17 — prio3_cash_scalp
// Structure: scalp · VWAP reclaim scalp on underlying
// Underlying: PRIO3
input
  StopTicks(7);
  TargetTicks(12);
var
  EntryPrice : Float;
begin
  EntryPrice := Close;
// scalp PRIO3 — VWAP reclaim scalp on underlying
// BuyAtMarket(100, "PRIO3");
  SetStopLoss(StopTicks * MinPriceIncrement);
  SetProfitTarget(TargetTicks * MinPriceIncrement);
end;
