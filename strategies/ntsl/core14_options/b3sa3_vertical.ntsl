// Arbitragem Core17 — b3sa3_vertical
// Structure: vertical · Bull call vertical spread
// Underlying: B3SA3
input
  StopTicks(5);
  TargetTicks(7);
var
  EntryPrice : Float;
begin
  EntryPrice := Close;
// Leg 1: buy lower strike call on B3SA3
// BuyAtMarket(1, "B3SAA140");
// Leg 2: sell higher strike call
// SellAtMarket(1, "B3SAA140");
  SetStopLoss(StopTicks * MinPriceIncrement);
  SetProfitTarget(TargetTicks * MinPriceIncrement);
end;
