// Arbitragem Core17 — itub4_cash_scalp
// Structure: scalp · VWAP reclaim scalp on underlying
// Underlying: ITUB4
input
  StopTicks(7);
  TargetTicks(10);
var
  EntryPrice : Float;
begin
  EntryPrice := Close;
// scalp ITUB4 — VWAP reclaim scalp on underlying
// BuyAtMarket(100, "ITUB4");
  SetStopLoss(StopTicks * MinPriceIncrement);
  SetProfitTarget(TargetTicks * MinPriceIncrement);
end;
