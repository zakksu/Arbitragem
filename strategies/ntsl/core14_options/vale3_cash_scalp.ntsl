// Arbitragem Core17 — vale3_cash_scalp
// Structure: scalp · VWAP reclaim scalp on underlying
// Underlying: VALE3
input
  StopTicks(7);
  TargetTicks(7);
var
  EntryPrice : Float;
begin
  EntryPrice := Close;
// scalp VALE3 — VWAP reclaim scalp on underlying
// BuyAtMarket(100, "VALE3");
  SetStopLoss(StopTicks * MinPriceIncrement);
  SetProfitTarget(TargetTicks * MinPriceIncrement);
end;
