// Arbitragem Core17 — bbdc4_cash_scalp
// Structure: scalp · VWAP reclaim scalp on underlying
// Underlying: BBDC4
input
  StopTicks(7);
  TargetTicks(6);
var
  EntryPrice : Float;
begin
  EntryPrice := Close;
// scalp BBDC4 — VWAP reclaim scalp on underlying
// BuyAtMarket(100, "BBDC4");
  SetStopLoss(StopTicks * MinPriceIncrement);
  SetProfitTarget(TargetTicks * MinPriceIncrement);
end;
