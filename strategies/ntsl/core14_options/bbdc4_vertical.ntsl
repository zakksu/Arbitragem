// Arbitragem Core17 — bbdc4_vertical
// Structure: vertical · Bull call vertical spread
// Underlying: BBDC4
input
  StopTicks(5);
  TargetTicks(11);
var
  EntryPrice : Float;
begin
  EntryPrice := Close;
// Leg 1: buy lower strike call on BBDC4
// BuyAtMarket(1, "BBDCA150");
// Leg 2: sell higher strike call
// SellAtMarket(1, "BBDCA150");
  SetStopLoss(StopTicks * MinPriceIncrement);
  SetProfitTarget(TargetTicks * MinPriceIncrement);
end;
